# Audit & QA — session du 10 août 2026

Récapitulatif de tout ce qui a été fait aujourd'hui, vérifié, et ce qu'il reste à faire.
Généré automatiquement en fin de session — à jour au commit correspondant.

---

## 1. Fait et vérifié (lint + build passent)

### Paiement — FeexPay
- [x] Intégration FeexPay activée en production, mode **sandbox** (clés `test_...`).
- [x] Variables poussées sur Vercel (production) : `FEEXPAY_ENABLE`, `FEEXPAY_SHOP_ID`,
      `FEEXPAY_API_KEY`, `FEEXPAY_MODE`, `FEEXPAY_CALLBACK_URL`, `FEEXPAY_WEBHOOK_SECRET`,
      `DEMO_PAYMENTS=false`.
- [x] Vérification webhook durcie (comparaison à temps constant).
- [x] Script `scripts/push-vercel-env.mjs` corrigé (bug de corruption d'URL de connexion
      sur Windows via `--value` shell — repassé en stdin, `production` uniquement).
- [x] **Règle métier ajoutée** : une commande dont le paiement n'est pas confirmé
      (`recue`) n'apparaît **plus jamais** côté admin — ni dans le Kanban, ni dans les
      compteurs/KPI, ni dans le CA. Elle ne devient visible qu'une fois `paiement_confirme`.
      (`lib/admin/order-board.ts`, `lib/admin/kpis.ts`, `admin-orders-page.tsx`)

### Design — palette bleu Telegram / rouge / noir
- [x] Tokens centralisés (`app/globals.css` + `lib/design-tokens.ts`) : noir `#17181B`,
      bleu Telegram assombri `#0077B3` (accessible), rouge vif `#D62828`, fond crème inchangé
      (contrainte photo produit).
- [x] **Bug de contraste corrigé partout** : ~26 fichiers avaient `bg-accent text-text`
      (texte sombre codé en dur sur fond plein) — hérité de l'ancienne palette dorée,
      illisible avec le rouge vif. Remplacé par `text-accent-foreground` (blanc) partout,
      y compris le bouton **« Confirmer et payer »** du checkout.
- [x] Logo : sheen animé passé du doré/rouge au bleu (le rouge reste réservé aux CTA).
- [x] Reçu imprimable, manifestes PWA, thème navigateur mis à jour.

### Contenu accueil
- [x] Horaires corrigés : « Tous les jours sauf le lundi » (`lib/business-info.ts`).
- [x] Numéro WhatsApp corrigé — dérivé automatiquement du numéro de téléphone
      (`WHATSAPP_PICKUP` = `ORDER_PHONE`, une seule source, ne peut plus diverger).
- [x] Image hero remplacée par la photo fournie (`hero-coeur-signature.png`).
- [x] Bandeau CTA final (« Prêt à vous faire plaisir ? ») : fond sombre texturé
      (grain + lueur rouge/bleu très discrète), remplace le fond plat.
- [x] Nouvelle section **« Nos engagements »** (bandeau bleu clair, 4 valeurs — donnée
      `BRAND_VALUES` qui existait déjà mais n'était utilisée nulle part).
- [x] Nouvelle section **« Notre univers »** (3 cartes catégories : Glaces artisanales,
      Entremets signature, Cadeaux & bouquets → ce dernier lien filtre déjà le catalogue
      via `?cadeaux=1`).

### Back-office — CRUD commandes
- [x] Create (commande manuelle), Update (édition client + articles), Delete (suppression
      définitive limitée aux commandes non payées/annulées) ajoutés au Kanban existant.

### Analytics admin
- [x] Filtres période sur `/admin/cockpit` : **Aujourd'hui** (défaut) / Cette semaine /
      Ce mois-ci / Depuis la création — comparaison (« vs hier », « vs semaine dernière »…)
      adaptée à chaque période.

---

## 2. Bloquant — pas dans mes moyens, à faire par toi

- [ ] **`ahmesgouts.bj` pointe toujours vers l'ancien projet Vercel (`ah-mes-gouts`)**,
      pas vers `gift-entremets`. Tenté 3× en CLI, bloqué à chaque fois par le
      classificateur de permissions (action jugée trop sensible pour être automatisée).
      → Vercel Dashboard → projet `gift-entremets` → Settings → Domains → ajouter
      `ahmesgouts.bj` (il proposera de le détacher de l'autre projet — accepter).
      **Tant que ce n'est pas fait, rien de ce qui précède n'est visible sur ce domaine.**
      Utilise `https://gift-entremets.vercel.app` en attendant.
- [ ] Confirmer si la clé `fp_...` que tu as donnée est bien la clé **live** FeexPay
      (mise de côté, pas utilisée) avant de basculer `FEEXPAY_MODE=live`.

## 3. Pas fait — hors scope aujourd'hui, à discuter

- [ ] La maquette de référence envoyée montrait aussi une nav différente (« La création
      du jour », « Notre histoire », « Livraison ») — pas touché, la nav actuelle est
      restée telle quelle. Dis-moi si tu veux qu'on l'aligne.
- [ ] Fermeture le lundi : corrigé **à l'affichage uniquement**. Les créneaux de commande
      ne sont pas bloqués automatiquement un lundi (aucune vérification serveur). Si un
      client force une URL de commande un lundi, ça passerait quand même. À corriger si
      c'est un vrai risque (actuellement le menu du jour doit de toute façon être activé
      manuellement chaque jour, donc le risque réel dépend de vos habitudes).
- [ ] Risques de scalabilité déjà documentés (verrous créneaux / rate-limit en mémoire
      process, pas encore sur Redis) — non traités, voir `SIMPLICITE-ET-SCALABILITE.md`.
- [ ] Photos exactes de la maquette de référence (spoon doré, pétales, mise en scène)
      non répliquées — structure et contenu oui, mise en scène photo non (pas d'assets).

---

## 4. QA — vérifications faites

- `npm run lint` → 0 erreur (7 warnings préexistants, non liés à la session).
- `npm run build` → 34/34 pages générées, aucune erreur TypeScript/Next.
- Grep de sécurité : aucun hex codé en dur en dehors de `app/globals.css` +
  `lib/design-tokens.ts` (miroir JS volontaire) + couleurs de marques tierces
  (WhatsApp vert, volontairement laissées telles quelles).
- Contraste vérifié par calcul (pas à l'œil) pour chaque paire couleur/texte de la palette.

## À tester manuellement dès que le domaine est corrigé
1. Parcours complet catalogue → panier → checkout → paiement sandbox FeexPay → confirmation.
2. Vérifier qu'une commande dont le paiement échoue n'apparaît **pas** dans `/admin/commandes`.
3. `/admin/cockpit` : basculer les 4 filtres de période, vérifier que les chiffres changent.
4. Créer une commande manuelle depuis l'admin, l'éditer, la supprimer.
5. Vérifier l'image et le texte d'accueil (horaires, WhatsApp) sur mobile et desktop.
